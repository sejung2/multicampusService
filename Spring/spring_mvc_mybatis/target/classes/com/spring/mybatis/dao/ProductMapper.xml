<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- myBatis 에서는 현재 xml 문서가 Mapper의 역할을 한다는 정의와
     Mapper 태그의 문법을 정의하기 위한 DTD 정보를 요구함
     Mapper 문서에서는 반드시 Mapper를 표현하는 DTD 정보를 표현해야 함-->
<!-- 아래 mapper에서 구현되는 기능은 IProductDAO 추상메서드로 한정됨-->
<mapper namespace="com.spring.mybatis.dao.IProductDAO">
    <!-- resultMap: select 결과를 자바빈즈에 매핑하기 위한 설정 -->
    <resultMap id="prdResult" type="com.spring.mybatis.model.ProductVO">
        <result property="prdNo" column="prdNo"/>
        <result property="prdName" column="prdName"/>
        <result property="prdPrice" column="prdPrice"/>
        <result property="prdCompany" column="prdCompany"/>
        <result property="prdStock" column="prdStock"/>
        <result property="prdDate" column="prdDate"/>
    </resultMap>
<!-- 전체 상품 조회: select -->
    <select id="listAllProduct" resultMap="prdResult">
        SELECT * FROM product2 ORDER BY prdNO
    </select>
</mapper>